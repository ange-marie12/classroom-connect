from django.contrib import admin\nfrom .models import *\n\n# Register your models here.\n# For example, if you have a model named 'Student', you can register it like below:\n# admin.site.register(Student)\n\n# Register all models\nmodels = [\n    'Model1',  # Replace with your actual model names\n    'Model2',  # Add more models as needed\n]  \n\nfor model in models:\n    try:\n        admin.site.register(globals()[model])\n    except KeyError:\n        print(f'Error: {model} not found in the current namespace')
